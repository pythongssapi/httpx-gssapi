name: tests

on:
  pull_request:
  push:
    branches:
      - '*'
    tags:
      - 'v*'

jobs:

  flake8:
    name: Flake8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - name: Set Up Python
        uses: actions/setup-python@v7.0.0
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install flake8
      - name: Check
        run: python -m flake8

  mypy:
    name: MyPy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - name: Setup Build Tools
        run: |
          sudo apt-get update -qq
          sudo apt-get install --yes libkrb5-dev krb5-user krb5-kdc krb5-admin-server krb5-multidev
      - name: Set Up Python
        uses: actions/setup-python@v7.0.0
        with:
          python-version: '3.x'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install mypy .[all]
      - name: Check
        run: python -m mypy --install-types --non-interactive

  test:
    name: Test
    needs:
      - flake8
      - mypy
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python: [ '3.9', '3.10', '3.11', '3.12', '3.13', '3.14' , '3.15' ]
        httpx: [ 'httpx', 'httpx2' ]
    steps:
      - uses: actions/checkout@v7.0.1
      - name: Setup Build Tools
        run: |
          sudo apt-get update -qq
          sudo apt-get install --yes libkrb5-dev krb5-user krb5-kdc krb5-admin-server krb5-multidev
      - name: Set Up Python
        uses: actions/setup-python@v7.0.0
        with:
          allow-prereleases: true
          python-version: ${{ matrix.python }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade setuptools wheel
          python -m pip install --upgrade .[all] pytest-cov codecov
      - name: Run Tests
        env:
          HTTPX: ${{ matrix.httpx }}
        run: python -m pytest --cov=httpx_gssapi --cov-report=xml
      - name: Upload Coverage
        run: python -m codecov --token=${{ secrets.CODECOV_TOKEN }}

  publish:
    if: startsWith(github.ref, 'refs/tags/')
    name: Create Release
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v7.0.1
        with:
          fetch-depth: 0
      - name: Set Up Python
        uses: actions/setup-python@v7.0.0
        with:
          python-version: '3.x'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade setuptools wheel
      - name: Build
        run: python setup.py sdist bdist_wheel
      - name: Change Log
        id: change_log
        run: >-
          echo "::set-output name=text::$(
          git --no-pager log --pretty=format:'- %s (%h)'
          `git describe --abbrev=0 --tags HEAD^1`..HEAD
          )"
      - name: Create Release
        id: create_release
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ github.ref }}
          name: Release ${{ github.ref }}
          body: |
            Change Log:
            ${{ steps.change_log.outputs.text }}
          draft: true  # Give me a moment to fill in the description
          artifacts: "dist/*"
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Publish
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          user: __token__
          password: ${{ secrets.PYPI_TOKEN }}
